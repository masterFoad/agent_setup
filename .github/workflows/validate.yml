name: validate

on:
  push:
  pull_request:

jobs:
  unix:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - name: Bash syntax
        run: bash -n install-mac.sh packaging/macos/build-mac-dmg.sh packaging/macos/FOAD-Dev-Setup.command.template
      - name: ShellCheck 0.11.0
        run: |
          curl -fsSL https://github.com/koalaman/shellcheck/releases/download/v0.11.0/shellcheck-v0.11.0.linux.x86_64.tar.xz -o /tmp/shellcheck.tar.xz
          echo "8c3be12b05d5c177a04c29e3c78ce89ac86f1595681cab149b65b97c4e227198  /tmp/shellcheck.tar.xz" | sha256sum -c -
          tar -xJf /tmp/shellcheck.tar.xz -C /tmp
          /tmp/shellcheck-v0.11.0/shellcheck install-mac.sh packaging/macos/build-mac-dmg.sh packaging/macos/FOAD-Dev-Setup.command.template
      - name: Python checks
        run: |
          python3 -m py_compile packaging/macos/make-background.py
          python3 -m unittest discover -s tests -v
      - name: Git whitespace
        run: git diff --check "$(git hash-object -t tree /dev/null)" HEAD

  windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v5
      - name: Parse PowerShell scripts
        shell: pwsh
        run: |
          $failed = $false
          Get-ChildItem -Recurse -Filter *.ps1 | ForEach-Object {
            $tokens = $null
            $errors = $null
            [System.Management.Automation.Language.Parser]::ParseFile($_.FullName, [ref]$tokens, [ref]$errors) | Out-Null
            if ($errors.Count) {
              $errors | ForEach-Object { Write-Error "$($_.Extent.File):$($_.Extent.StartLineNumber): $($_.Message)" }
              $failed = $true
            }
          }
          if ($failed) { exit 1 }
      - name: Release contract tests
        run: python -m unittest discover -s tests -v
      - name: Persistent Windows PATH regression test
        shell: powershell
        run: .\tests\test_windows_path.ps1
      - name: Official Claude installer integration test
        shell: powershell
        run: |
          . .\install-windows.ps1 -LibraryOnly
          if (-not (Install-ClaudeCodeNative)) { throw "Claude installation did not complete." }
          if (-not (Get-VerifiedSignedClaudePath)) { throw "Claude Authenticode verification failed." }
          if (-not (Check-CommandVersion "claude" -PersistentPathOnly)) { throw "Claude is missing from persistent PATH." }
          $freshCheck = '$machinePath = [Environment]::GetEnvironmentVariable("Path", "Machine"); $userPath = [Environment]::GetEnvironmentVariable("Path", "User"); $env:Path = "$machinePath;$userPath"; claude --version'
          & "$env:SystemRoot\System32\WindowsPowerShell\v1.0\powershell.exe" -NoProfile -Command $freshCheck
          if ($LASTEXITCODE -ne 0) { throw "A fresh Windows PowerShell could not run Claude." }
      - name: Compile Windows preview package
        shell: pwsh
        run: |
          choco install innosetup --no-progress -y
          $env:FOAD_ALLOW_UNTAGGED_BUILD = "1"
          .\packaging\windows\build-windows-exe.ps1
          if (-not (Test-Path .\dist\windows\FOAD-Dev-Setup-Windows.exe.sha256)) { exit 1 }

  macos-package:
    runs-on: macos-14
    steps:
      - uses: actions/checkout@v5
      - name: Build and verify macOS preview package
        run: |
          FOAD_ALLOW_UNTAGGED_BUILD=1 ./packaging/macos/build-mac-dmg.sh
          cd dist/macos
          shasum -a 256 -c FOAD-Dev-Setup-macOS.dmg.sha256
